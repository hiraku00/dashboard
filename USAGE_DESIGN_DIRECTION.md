## 方向宣言: 使用量・上限

- ジョブ: 3つの外部サービスの利用状況と上限を、ひと目で比較し、必要なら詳細まで追えるようにする。
- レジスター: 主=精密、副=なし。運用データを落ち着いて判断する画面であり、装飾より数値と構造を優先する。
- サーフェス: web（ダッシュボード）。
- パレット: 地 #eef1f7 / 面 #ffffff / 文字 #20242b / 補助 #6f7682 / アクセント #0a84ff（選択・進捗のみ）。
- タイポ: 既存の日本語サンセリフ。数値は tabular-nums、本文は16pxを下限にする。
- 余白リズム: 8pxグリッド。PCのカードは画面高に合わせて 620–800px、内部の主要ブロック間は24px、詳細行は16px。
- モーション: 160ms ease-out。状態変化のみ、装飾的な演出はなし。
- シグネチャー: 3つのカードが同じ縦のリズムで「主数値 → 使用率 → 説明 → 内訳」を読ませる。
- 禁じ手: 固定高さに情報を押し込むこと、4分割の細い数値列、説明文の成り行き改行・省略。
