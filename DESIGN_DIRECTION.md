## 方向宣言: Watch List

- ジョブ: 一覧からタイトル・人物・追加日・状態を横断比較し、目的のリンクを1操作で開く。
- レジスター: 主=精密、副=なし（見せるためのコピーを排し、個人の操作道具として静かな一貫性を優先する）。
- サーフェス: web app。
- パレット: 地 `#EEF0F5` / 面 `#FFFFFF` / 文字 `#1D1D1F` / アクセント `#0A84FF`（追加・リンク・フォーカスのみ）/ 成功 `#16824A` / 警告 `#8B5D00` / 危険 `#C52A37`。
- タイポ: display/body=-apple-system（Appleのプラットフォーム慣習と可読性を狙って意図的に採用）、和文=Hiragino Sans、数値=tabular-nums。スケール比=1.2。
- 余白リズム: 8pxグリッド、要素間=8/16px、セクション間=32/48px。
- モーション: 150ms ease-out、状態変化だけに限定。reduced-motion 時は即時表示。
- シグネチャー: 行と列を揃えた台帳型一覧。タイトルは一行で省略して密度を保ち、クリックで編集する。外部リンクは独立した「リンク」列に全件を表示し、状態は行内のプルダウンで更新する。
- 一覧の範囲: 10件ずつ表示し、総件数・現在の範囲・ページ下部の最初・前・現在ページ前後・次・最後を明示して、全件を見失わない。
- 密度: デスクトップでは内容メモ1行を含む固定高の10行とページングを1画面に収める。統計は1行の補助情報に圧縮し、リンクは行内で折り返さない。
- 禁じ手: 一律のカード枠／派手なグラデーション・グロー／絵文字アイコン／マーケティング用ヒーロー・副題／列の意味が曖昧なカード羅列。

### リデザイン診断

1. 旧画面は統計とコピーの視覚重量が強く、最頻ジョブである「確認してリンクを開く」より先に目に入っていた。
2. 人物名と追加日がタイトル下の補助情報に埋もれ、複数件を比較するための視線移動が多かった。
3. Apple HIGの「目的・階層・親しみやすい既知の操作・明確なフィードバック」に沿い、人物・追加日・状態を独立列にした台帳型一覧と直接リンクを主役に再編する。
