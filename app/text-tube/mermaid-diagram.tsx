"use client";
import { useEffect, useRef, useState } from "react";
export function MermaidDiagram({chart}:{chart:string}){const ref=useRef<HTMLDivElement>(null);const [error,setError]=useState("");useEffect(()=>{let mounted=true;import("mermaid").then(async({default:mermaid})=>{mermaid.initialize({startOnLoad:false,securityLevel:"strict",theme:"base",themeVariables:{primaryColor:"#e8f1ff",primaryTextColor:"#202027",primaryBorderColor:"#1167d8",lineColor:"#5b6472"}});try{const id=`mermaid-${Math.random().toString(36).slice(2)}`;const rendered=await mermaid.render(id,chart);if(mounted&&ref.current)ref.current.innerHTML=rendered.svg}catch(e){if(mounted)setError(e instanceof Error?e.message:"Mermaidを描画できませんでした。")}}).catch(()=>setError("Mermaidライブラリを読み込めませんでした。"));return()=>{mounted=false}},[chart]);return <div className="mermaid-card" ref={ref}>{error?<><p className="mermaid-error">Mermaidを描画できませんでした。</p><pre><code>{chart}</code></pre></>:<p className="muted-copy">図を描画中…</p>}</div>}
